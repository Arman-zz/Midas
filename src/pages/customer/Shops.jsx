import { useMemo,useState } from 'react'
import ShopCard from '../../components/cards/ShopCard'
import { shops } from '../../data/appData'
const coords={dhanmondi:[23.7465,90.376],jigatola:[23.7389,90.3733],gulshan:[23.7925,90.4078],uttara:[23.8759,90.3795],mirpur:[23.8223,90.3654],bashundhara:[23.819,90.4526]}
const distance=(a,b,c,d)=>{const r=6371,toRad=x=>x*Math.PI/180;const p=toRad(c-a),q=toRad(d-b);const z=Math.sin(p/2)**2+Math.cos(toRad(a))*Math.cos(toRad(c))*Math.sin(q/2)**2;return 2*r*Math.asin(Math.sqrt(z))}
export default function Shops({globalSearch=''}){const [mode,setMode]=useState('nearby');const [area,setArea]=useState(localStorage.getItem('midas-customer-area')||'Dhanmondi, Dhaka');const origin=Object.entries(coords).find(([name])=>area.toLowerCase().includes(name))?.[1]||coords.dhanmondi;const list=useMemo(()=>shops.filter(s=>`${s.name} ${s.area}`.toLowerCase().includes(globalSearch.toLowerCase())).map(s=>({...s,distance:distance(origin[0],origin[1],s.lat,s.lng)})).sort((a,b)=>mode==='rating'?b.rating-a.rating:a.distance-b.distance),[globalSearch,mode,area]);const change=()=>{const next=prompt('Enter your area or neighborhood',area)?.trim();if(next){localStorage.setItem('midas-customer-area',next);setArea(next)}};return <><div className="section-h"><div><h2>Partner Shops Near You</h2><p className="lead">Sorted from <b>{area}</b>. <button className="text-button inline-text-button" onClick={change}>Change area</button></p></div><div className="tabbar"><button className={mode==='nearby'?'active':''} onClick={()=>setMode('nearby')}>Nearby</button><button className={mode==='rating'?'active':''} onClick={()=>setMode('rating')}>Highest rated</button></div></div><div className="grid g-3">{list.map(shop=><ShopCard shop={shop} key={shop.id}/>)}</div></>}
