import { useState } from 'react'
import { shops } from '../../data/appData'
import { useToast } from '../../context/ToastContext'
export default function Profile(){const [data,setData]=useState(shops[0]);const notify=useToast();const submit=e=>{e.preventDefault();localStorage.setItem('midas-shop-profile',JSON.stringify(data));notify('Shop profile saved')};return <form className="card settings-card" onSubmit={submit}><div className="card-head"><div><div className="card-title">Shop Profile</div><div className="card-sub">Details shown to customers in Nearby Shops and Marketplace.</div></div></div><div className="card-pad"><div className="field-row"><label className="field-label">Shop name</label><input className="field" value={data.name} onChange={e=>setData({...data,name:e.target.value})}/></div><div className="field-row"><label className="field-label">Full location</label><textarea className="field" value={data.area} onChange={e=>setData({...data,area:e.target.value})}/></div><button className="btn btn-gold">Save shop profile</button></div></form>}
